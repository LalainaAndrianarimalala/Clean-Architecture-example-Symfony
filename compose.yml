services:
  php:
    build: .
    ports:
      - "8080:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - ./monprojet/:/app
    environment:
      - SERVER_NAME=localhost
      - REDIS_HOST=redis      
  redis:
    image: redis:alpine
    ports:
      - "6380:6379"